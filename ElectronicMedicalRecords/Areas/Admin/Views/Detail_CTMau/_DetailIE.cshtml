@model ElectronicMedicalRecords.Models.MultiplesModel

<div class="card-body">
    <table class="table table-striped tableLoad" id="tableCTMaus" style="width:100%">
        <thead>
            <tr>
                <th hidden></th>
                <th hidden></th>
                <th>Chỉ Định</th>
                <th>Tên Xét Nghiệm</th>
                <th>Kết Quả</th>
                <th>Chỉ Số Cân Bằng</th>
                <th>Đơn Vị</th>
                <th>Máy XN</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.CTMau.Count(); i++)
            {
                <tr>
                    <td hidden>
                        <input value="@Model.Detail_CTMaus[i].InformationExamination_ID" type="text" name="Detail_CTMaus[@i].InformationExamination_ID" hidden />
                        <input value="@Model.Detail_CTMaus[i].CTMau_ID" type="text" name="Detail_CTMaus[@i].CTMau_ID" hidden />
                    </td>
                    <td hidden>
                        @*<input value="@ViewBag.CTMau[i].ID" type="text" name="CTMau[@i].ID" hidden />*@
                        <input value="@Model.Detail_CTMaus[i].ID" type="text" name="Detail_CTMaus[@i].ID" hidden />
                    </td>
                    <td>
                        @*<input type="checkbox" value="false" class="form-check-input form-check-primary" name="CTMau[@i].ChiDinh" id="customColorCheck-@ViewBag.CTMau[i].ID" onchange="updateCountTests(@ViewBag.CTMau[i].ID, event)">*@
                        <input hidden checked type="checkbox" value="true" class="form-check-input form-check-primary" data-id="@Model.CTMau[i].ID" name="Detail_CTMaus[@i].ChiDinh">
                        <input disabled checked type="checkbox" value="true" class="form-check-input form-check-primary">
                    </td>
                    <td>
                        <label class="form-check-label">@Model.CTMau[i].NameTest</label>
                    </td>
                    <td>
                        @Model.Detail_CTMaus[i].Result
                    </td>
                    <td>
                        @Model.CTMau[i].CSBT
                    </td>
                    <td>
                        @Model.CTMau[i].Unit
                    </td>
                    <td>
                        @Model.CTMau[i].MayXN
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
